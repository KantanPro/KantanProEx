---
description: KantanPro 無料版 ZIP + KantanProEX 有料版 GitHub Release の配布一式リリース手順
alwaysApply: false
---

# 配布一式リリース（B: フリー版 ZIP + EX Release）

ユーザーが「リリース」「配布一式」「B.」等で依頼したとき、**無料版 ZIP と有料版 GitHub Release の両方**を行う。有料版のみの依頼でない限り、無料版を省略しない。

**どちらも GitHub Release だけで終わらせない。** サイトが配っているファイルを差し替えるまでがリリース。
- 有料版 → 「C. WooCommerce 配布ファイルの差し替え」（購入者のダウンロードが古いままになる）
- 無料版 → 「D. Download Monitor 配布ファイルの差し替え」（サイトのダウンロードボタンが古いままになる）

## リポジトリ（2つ）

| 版 | ローカルパス | GitHub |
|----|-------------|--------|
| 無料版 KantanPro | `wordpress/wp-content/plugins/KantanPro` | `KantanPro/KantanPro`（**GitHub Release + ZIP 1件**） |
| 有料版 KantanProEX | `wordpress/wp-content/plugins/KantanProEX` | `KantanPro/KantanProEx` |

バージョン番号は別系統（例: 無料 `1.2.x` / 有料 `1.3.x`）。同一機能変更時は**両方**バージョンを上げる。

## リリース完了の明記（必須）

回答冒頭に必ず:

- **✅ リリース完了** — 無料 ZIP + 有料 4 ZIP + GitHub Release まで実施
- **❌ リリース未実施** — 途中で止まっている、または片方のみ

## 手順概要

### 0. 作業前チェック（両リポジトリ）

各リポジトリで:

- ルートに `*.zip` なし / `wp-cli.phar` 未追跡
- `git status` → 未コミットがあればコミット → `git push origin main`

### A. 無料版 KantanPro（ZIP + GitHub Release）

1. **バージョン更新**
   - `ktpwp.php` の `Version`
   - `readme.txt` の `Stable tag` + changelog（日本語）
   - `plugin_config.json` の `default_version` を同じバージョンに更新
2. **コミット・push**（日本語メッセージ、例: 「〇〇を修正（v1.2.92）」）
3. **ZIP 生成**
   ```bash
   cd /Users/kantanpro/Desktop/KantanPro/wordpress/wp-content/plugins/KantanPro
   ./create_release_zip.sh
   ```
   → `/Users/kantanpro/Desktop/KantanPro_TEST_UP/KantanPro_{version}_{date}.zip`
4. **GitHub Release**
   - `git tag v{version} && git push origin v{version}`
   - `gh release create v{version} --repo KantanPro/KantanPro`
   - タイトル例: `KantanPro（WP）v{version} をリリースしました`
   - asset に `KantanPro_{version}_{date}.zip` を添付
5. **Download Monitor の配布ファイルを差し替え** — 「D. Download Monitor 配布ファイルの差し替え」を実施
6. **合格基準（無料版）**
   - `unzip -t` OK / 3MB 未満
   - ルートフォルダ名: `KantanPro`（単一）
   - `ktpwp.php` + `readme.txt` あり
   - **`create_dummy_data.php` を含む**（無料版は必須）
   - 含まない: `.md`, `vendor/`, `*.po`, `*.pot`, 内包 `*.zip`, `.cursor/`, `wp-cli.phar`

### B. 有料版 KantanProEX（4 ZIP + GitHub Release）

1. **バージョン更新** — `ktpwp.php` / `readme.txt`（changelog 日本語）
2. **コミット・push**（日本語メッセージ）
3. **4 エディション ZIP 生成**
   ```bash
   cd /Users/kantanpro/Desktop/KantanPro/wordpress/wp-content/plugins/KantanProEX
   ./scripts/build-all-release-zips.sh
   ```
   → `/Users/kantanpro/Desktop/KantanProEX_TEST_UP/`
4. **合格基準（各 EX ZIP）**
   - `unzip -t` OK / 3MB 未満 / `ktpwp.php` + `readme.txt` あり
   - 含まない: `.md`, `vendor/`, `create_dummy_data.php`, `*.po`, `*.pot`, 内包 `*.zip`
   - `KTPWP_EDITION` / `KTPWP_STAFF_LIMIT` / `KANTANPRO_PLUGIN_NAME` がエディションどおり
5. **GitHub Release**
   - `git tag v{version} && git push origin main && git push origin v{version}`
   - `gh release create v{version} --repo KantanPro/KantanProEx`
   - タイトル例: `KantanProEX（WP）v{version} をリリースしました`
   - asset に 4 ZIP 必須（pro / solo / team / business）
   - solo / team / business は対応 asset 必須（ワンクリック更新用）
6. **WooCommerce の商品を更新（必須）** — 下の「C. WooCommerce 配布ファイルの差し替え」を実施。
   これをやらないと**購入者は古いバージョンをダウンロードし続ける**。

### C. WooCommerce 配布ファイルの差し替え（有料版のリリース時は必ず実施）

kantanpro.com の販売ページが配っている ZIP を、新しいバージョンに差し替える。
GitHub Release だけでは購入者のダウンロードは古いままなので、**ここまでやって初めてリリース完了**。

対象商品: **ID 16297**「【買い切り】KantanProEX（WP）」（variable product / バリエーション4件）

| バリエーション | ID | ダウンロード ID（UUID・**変更禁止**） | ZIP |
|---|---|---|---|
| フルアクセス版 | 18308 | `00f13b46-2e1a-4dc8-a985-ca97ff36669b` | `KantanProEX_{ver}_{date}.zip` |
| ビジネス版 | 18307 | `86fcd242-39cd-428b-afa2-4f116ecc92e2` | `KantanProEXbusiness_{ver}_{date}.zip` |
| チーム版 | 18306 | `3f683d45-23bf-42e9-b0f2-5eeb857dcd7d` | `KantanProEXteam_{ver}_{date}.zip` |
| ソロ版 | 18305 | `257c57be-dc89-4b53-a7a9-39ac73bcc96f` | `KantanProEXsolo_{ver}_{date}.zip` |

認証は `~/.kantanpro/wp-kantanpro.env`（`WP_SITE` / `WP_USER` / `WP_APP_PASSWORD`）。
**アプリケーションパスワードには空白が含まれるので `source` せず、`grep`＋`cut` で取り出すこと。**

#### 1. 保護ディレクトリへアップロード（4本）

```bash
curl -s -A "$UA" -u "$USER:$PASS" \
  -F "file=@${ZIP};type=application/zip" \
  -F "type=downloadable_product" \
  "$SITE/wp-json/wp/v2/media"
```

- **`type=downloadable_product` を必ず付ける。** 付けないと公開ディレクトリに落ち、直リンクで
  誰でも落とせる（＝課金回避される）。`source_url` に `woocommerce_uploads` が入っているか毎回確認する。
- **`-A` でブラウザの User-Agent を付ける。** Cloudflare が CLI の UA の POST を 1010 で弾く。
  GET は通るので原因を見失いやすい。

#### 2. 各バリエーションの配布ファイルを差し替え

```bash
curl -s -A "$UA" -u "$USER:$PASS" -X PUT -H 'Content-Type: application/json' \
  -d '{"downloads":[{"id":"<UUID>","name":"<新ZIP名>","file":"<新URL>"}]}' \
  "$SITE/wp-json/wc/v3/products/16297/variations/<バリエーションID>"
```

- **UUID は絶対に変えない。** 変えると購入済みユーザーのダウンロード権限レコードと紐づかなくなる。
  更新するのは `name` と `file` だけ。
- 旧バージョンのファイルは**消さない**。

#### 3. 確認

- 4バリエーションとも `name` / `file` が新バージョンで、UUID が変わっていないこと
- 新 ZIP の直リンクが **403** であること（保護されている＝課金回避されない）
- 旧バージョンの直リンクも 403 のまま残っていること

### D. Download Monitor 配布ファイルの差し替え（無料版のリリース時は必ず実施）

kantanpro.com が配っている無料版 ZIP を差し替える。GitHub Release だけでは
サイトのダウンロードボタンは古いままなので、**ここまでやって初めてリリース完了**。

対象: ダウンロード投稿 **17694**「KantanPro（WP）無料版」/ バージョン行 **17696**（**行を増やさない**）
配布リンク: `https://www.kantanpro.com/download/17694/`

**REST では上げられない。** DLM の Admin クラスは `is_admin()` のときしか読まれず、
`upload_dir` フィルタが効かないため公開ディレクトリに落ちる。9444 の永続ブラウザを使う
（`~/.kantanpro/bin/cdp-wp.py`。`wp_ajax_dlm_upload_file` は DLM 側の cap 名タイプミスで必ず落ちるので使わない）。

#### 1. 保護ディレクトリへアップロード

```bash
# 管理画面を開いて nonce を取る
python3 ~/.kantanpro/bin/cdp-wp.py goto "https://www.kantanpro.com/wp-admin/post.php?post=17694&action=edit"
python3 ~/.kantanpro/bin/cdp-wp.py eval "_wpPluploadSettings.defaults.multipart_params._wpnonce"

COOKIES=$(python3 ~/.kantanpro/bin/cdp-wp.py cookies "https://www.kantanpro.com/wp-admin/")
curl -s -A "$UA" -b "$COOKIES" \
  -F "name=KantanPro_{ver}_{date}.zip" \
  -F "action=upload-attachment" \
  -F "_wpnonce=<上で取った nonce>" \
  -F "type=dlm_download" \
  -F "async-upload=@${ZIP};type=application/zip" \
  "https://www.kantanpro.com/wp-admin/async-upload.php"
```

- **`type=dlm_download` を必ず付ける**（付けないと公開ディレクトリに落ちる）
- **ブラウザの User-Agent を付ける**（Cloudflare が CLI の UA の POST を 1010 で弾く）
- 応答の `data.url` に `dlm_uploads` が入っているか確認する

#### 2. バージョン行を更新（新しい行を作らない）

編集画面のフィールドを書き換えて `#publish` を押す。**`downloadable_file_id[0]` = 17696 は触らない。**
行を分けるとダウンロード数がリセットされる。

```js
document.querySelector('textarea[name="downloadable_file_urls[0]"]').value = '<新URL>';
document.querySelector('input[name="downloadable_file_version[0]"]').value = '{ver}';
document.querySelector('#publish').click();
```

#### 3. 確認

- バージョン行が **1行のまま**、`downloadable_file_id[0]` が 17696 のまま
- 新旧どちらの直リンクも **403**
- `https://www.kantanpro.com/download/17694/` が新しい ZIP を返す（同梱バージョンを確認。DL数が1件増える）

### 6. 完了報告（必須・両方）

**無料版**

1. バージョン（旧 → 新）、リリース種別
2. **GitHub Release URL** / zipballUrl / asset 1件の確認
3. ローカル ZIP パス
4. 解凍後フォルダ名（`KantanPro`）
5. コミットハッシュ（`KantanPro` リポジトリ）

**有料版**

1. バージョン（旧 → 新）、リリース種別
2. GitHub Release URL
3. zipballUrl の有無
4. assets 4 件の確認
5. ローカル ZIP パス一覧・解凍後フォルダ名
6. コミットハッシュ（`KantanProEx` リポジトリ）

## チャット用プロンプト（コピペ）

### A. EX のみ GitHub Release

```
>#########################################
A. EX のみ GitHub Release
>#########################################

KantanProEX（有料版）のみリリースしてください。無料版 KantanPro は対象外です。

@KantanProEX/.cursor/rules/release-workflow-bundle.mdc の「B. 有料版 KantanProEX」に従って実施。

## 前提
- リポジトリ: `wordpress/wp-content/plugins/KantanProEX`
- GitHub: `KantanPro/KantanProEx`
- コミットメッセージは日本語
- コード変更がある場合のみバージョンを +1（変更なしならリリースしない）

## バージョン更新
- `ktpwp.php` の `Version`
- `readme.txt` の `Stable tag` + changelog（日本語）

## 手順
1. 作業前: ルートに `*.zip` なし / `wp-cli.phar` 未追跡
2. `git status` → 未コミットがあればコミット → `git push origin main`
3. `./scripts/build-all-release-zips.sh`
   → `/Users/kantanpro/Desktop/KantanProEX_TEST_UP/`
4. `git tag v{version} && git push origin v{version}`
5. `gh release create v{version} --repo KantanPro/KantanProEx`
   - タイトル: `KantanProEX（WP）v{version} をリリースしました`
   - asset: 4 ZIP（pro / solo / team / business）
6. **WooCommerce の商品（ID 16297）の配布ファイルを 4 バリエーションとも差し替える**
   → 「C. WooCommerce 配布ファイルの差し替え」参照。ここまでやらないとリリース完了ではない

## 回答形式（必須）
- 冒頭: **✅ リリース完了** または **❌ リリース未実施**
- バージョン（旧 → 新）、リリース種別
- GitHub Release URL / zipballUrl / assets 4件の確認
- ローカル ZIP パス一覧、解凍後フォルダ名
- コミットハッシュ
- **WooCommerce 商品の差し替え結果**（4バリエーションの新ファイル名・UUID 維持・直リンク 403）
```

### B. フリー版 ZIP + EX Release まとめて（配布一式）

```
>#########################################
B. フリー版 ZIP + EX Release まとめて（配布一式）
>#########################################

無料版 KantanPro と有料版 KantanProEX の両方をリリースしてください。

@KantanProEX/.cursor/rules/release-workflow-bundle.mdc に従って実施。

## 前提
| 版 | ローカルパス（絶対） | GitHub | asset |
|----|---------------------|--------|-------|
| 無料版 | `/Users/kantanpro/Desktop/KantanPro-free-test/wp-content/plugins/KantanPro` | `KantanPro/KantanPro` | ZIP 1件 |
| 有料版 | `/Users/kantanpro/Desktop/KantanPro/wordpress/wp-content/plugins/KantanProEX` | `KantanPro/KantanProEx` | ZIP 4件（pro / solo / team / business） |

- コミットメッセージは日本語
- 機能変更・バグ修正がある場合は**両方**バージョンを上げる
  - 無料: `ktpwp.php` / `readme.txt`（Stable tag + changelog）/ `plugin_config.json` の `default_version` の**3ファイルすべて**
  - 有料: `ktpwp.php` / `readme.txt`（Stable tag + changelog）
- バージョン番号は別系統（無料 1.3.x / 有料 1.4.x）
- ローカルの `Version` が GitHub の最新 Release より進んでいる（＝未リリースの版がある）場合は、その番号を再利用せず**次の番号へ繰り上げる**

## 手順
1. **作業前（両リポジトリ）**: ルートに `*.zip` なし / `wp-cli.phar` 未追跡 / 未 push コミットなし → 未コミットがあればコミット → `git push origin main`
2. **無料版 KantanPro**
   - バージョン更新（3ファイル）→ コミット → `git push origin main`
   - `./create_release_zip.sh` → `/Users/kantanpro/Desktop/KantanPro_TEST_UP/KantanPro_{version}_{date}.zip`
   - `git tag v{version} && git push origin v{version}`
   - `gh release create v{version} --repo KantanPro/KantanPro --title "KantanPro（WP）v{version} をリリースしました"` に asset 1件を添付
   - **Download Monitor（投稿 17694 / バージョン行 17696）を更新** — 「D.」を実施。ここまでやって無料版のリリース完了
3. **有料版 KantanProEX**
   - バージョン更新（2ファイル）→ コミット → `git push origin main`
   - `./scripts/build-all-release-zips.sh` → `/Users/kantanpro/Desktop/KantanProEX_TEST_UP/` に4本
   - `git tag v{version} && git push origin v{version}`
   - `gh release create v{version} --repo KantanPro/KantanProEx --title "KantanProEX（WP）v{version} をリリースしました"` に asset 4件を添付
   - 公開時に `.github/workflows/release-assets.yml` が同名 ZIP を再ビルドして上書きするため、`gh run list` で success を確認する（サイズが数百バイト変わるのは正常）
   - **WooCommerce の商品（ID 16297）を更新** — 「C. WooCommerce 配布ファイルの差し替え」を実施。
     4バリエーションとも新 ZIP に差し替え、UUID は変えない。ここまでやって有料版のリリース完了

## 検証（報告前に必ず実行）
両版の ZIP に対して:
- `unzip -t` OK / 3MB 未満 / ルートフォルダ単一
- `ktpwp.php` + `readme.txt` あり、同梱 `Version` が新バージョンと一致
- 含まない: `.md`, `vendor/`, `*.po`, `*.pot`, 内包 `*.zip`, `.cursor/`, `wp-cli.phar`
- 無料版のみ: `create_dummy_data.php` を**含む**
- 有料版のみ: `create_dummy_data.php` を**含まない** / `KTPWP_EDITION`・`KTPWP_STAFF_LIMIT`・`KANTANPRO_PLUGIN_NAME` がエディションどおり（pro:0 / solo:1 / team:5 / business:15）
- 有料版のみ: WooCommerce 商品 16297 の4バリエーションが新 ZIP を指していること／UUID が変わっていないこと／新 ZIP の直リンクが 403 であること

## 回答形式（必須）
- 冒頭: **✅ リリース完了** または **❌ リリース未実施**

**無料版**
- バージョン（旧 → 新）、リリース種別
- GitHub Release URL / zipballUrl / asset 1件
- ローカル ZIP パス、解凍後フォルダ名（`KantanPro`）
- コミットハッシュ
- **Download Monitor の差し替え結果**（バージョン行 17696 のまま／直リンク 403／配布リンクが新 ZIP を返すこと）

**有料版**
- バージョン（旧 → 新）、リリース種別
- GitHub Release URL / zipballUrl / assets 4件
- ローカル ZIP パス一覧、解凍後フォルダ名
- コミットハッシュ
- **WooCommerce 商品の差し替え結果**（4バリエーションの新ファイル名・UUID 維持・直リンク 403）
```

## 注意

- 有料版のみのリリース依頼（「KantanProEX のみ」等）のときだけ A を省略可
- 同一機能を両方に入れた場合、**有料だけリリースして無料を忘れない**
- `create_release_zip.sh`（EX）は `#!/usr/bin/env bash` 必須（CI 互換）
- **有料版のリリースは、GitHub Release で終わりではない。** WooCommerce の商品を更新するまでは、
  購入者のダウンロードは古いバージョンのまま（[[C. WooCommerce 配布ファイルの差し替え]]）
